# Knowledge reuse

Prompt: Before planning, reuse any verified project or general knowledge about this subsystem.

Expected contract: run `knowledge-query` as a 只读 lookup. Verified records may guide the radar and planning; tentative, invalidated or replaced records remain historical and do not silently become requirements.
