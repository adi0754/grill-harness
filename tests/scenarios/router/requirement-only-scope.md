# Requirement-only scope

Prompt: Use Grill Harness only to clarify and baseline requirements. Do not compare or select implementation routes.

Expected contract: route to `grh-start`, maintain the adaptive radar and requirements baseline, then respect the narrower scope and stop. 不得进入路线选择，也不得自动串联 `grh-plan`。
