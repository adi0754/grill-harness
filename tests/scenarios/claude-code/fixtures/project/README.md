# Runtime scenario fixture

This deliberately small project gives fresh agents real files, symbols, tests,
contracts, and contradictory workflow artifacts to inspect. The runner copies
it into a temporary Git repository and removes write permission before invoking
Claude Code.
