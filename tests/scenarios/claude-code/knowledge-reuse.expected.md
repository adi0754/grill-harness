# Expected behavior: knowledge reuse

- Uses read-only `knowledge-query` across project and general stores.
- Allows only verified records to guide planning.
- Keeps tentative, invalidated, and replaced records as non-authoritative history.
