# Expected behavior: requirement-only scope

- Uses `grh-start` and the adaptive requirements radar before baseline approval.
- Preserves the explicit narrower scope.
- Stops after the approved requirements baseline without route selection, planning, or implementation.
