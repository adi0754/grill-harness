# Expected behavior: upstream read only

- Produces a compatibility report with `actions_performed: false` and `accepted_upstream_changes: false`.
- Does not install, update, overwrite, or accept upstream changes.
- Leaves any manual update outside the entry and recommends a later recheck.
