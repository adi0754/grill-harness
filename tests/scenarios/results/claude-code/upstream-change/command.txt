cwd=<TEMP_ROOT>/project
HOME=<TEMP_ROOT>/home
CLAUDE_CONFIG_DIR=<TEMP_ROOT>/home/.claude
command="/opt/homebrew/bin/claude" "-p" "--no-session-persistence" "--output-format" "json" "--permission-mode" "dontAsk" "--allowedTools" "Read,Glob,Grep,Bash,Write,Edit" "--max-budget-usd" "0.25" "请使用 /grill-harness 检查上游能力变化。读取 previous-manifest.json 和 current-facts.json，执行离线兼容检查，分类行为契约变化并给出建议。只生成兼容结论，不安装、不更新、不接受变化。\n\n项目绝对路径：<TEMP_ROOT>/project\n旧清单：<TEMP_ROOT>/project/workflow/upstream/previous-manifest.json\n当前事实：<TEMP_ROOT>/project/workflow/upstream/current-facts.json\n\n"
