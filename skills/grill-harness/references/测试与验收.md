# 测试与验收

实施证据必须包含实际命令、绝对工作目录、时间、退出码、原始输出绝对路径、Git 基线和对应 `REQ/DEC/TASK/ISSUE`。检查完整 diff、未提交文件和越权范围。

审查分两条独立轴：Standards 检查正确性、安全、维护性、性能、错误处理、测试质量和仓库规范；Spec 检查需求、决策、非目标、范围、兼容性及未验证假设。两轴都必须读真实完整文件和 diff。

最终验收必须由新本地会话从需求基线和最终规格重新理解目标，并实际运行可执行命令。结论仅为：验收通过、有条件通过、验收不通过、无法验证。证据缺失、过期或基线不符时不得无条件通过。
