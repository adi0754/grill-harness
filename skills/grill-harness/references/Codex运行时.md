# Codex 运行时

在新的本地 Codex 任务中发送角色任务协议中的短启动提示词，任务包路径与项目工作目录必须是绝对路径。Codex 可读本机文件并执行任务包授权的命令；不得依赖原会话历史。

若使用子 Agent，记录其独立性级别；子 Agent 不等于不同模型验证。创建 worktree、分支或破坏性 Git 操作必须另有用户授权。完成时只写指定报告，不能直接把 Grill Harness 状态标为通过。
