# 最终验收任务

## 给用户的话

### 30 秒结论：用当前真实仓库判断能否交付

验收者会在新的独立会话里，从需求和最终规格重新理解目标。结论以明白中文说明是否通过、有哪些限制或为何无法验证。没有当前命令、完整差异和有效基线证据时不能无条件通过。验收只读，不会顺手修复实现。

## 验收内容

- 角色与目标：新本地 Agent 会话独立验收
- 被验收分支 / worktree：
- 并行组、共享契约、迁移和生成物检查：
- 允许修改范围：只读
- 禁止修改范围：产品代码与状态批准记录
- 停止条件：证据无效、输入缺失、基线不符或命令无法执行

## 验收判定

- 中文主判定：通过 / 有条件通过 / 不通过 / 无法验证
- 对用户的理由、限制和发布阻塞项：

## 证据与标识

- 项目工作目录（绝对路径）：
- 当前工作流目录（绝对路径，仅限 `~/.grill-harness/`）：
- Git 基线：
- 输入产物（逐项绝对路径与版本/基线）：需求基线、最终规格、集成报告、最终仓库、完整 diff、未提交文件
- 实际命令与证据：`EVD-001`
- 机器码附注：`PASS` / `PASS_WITH_LIMITATION` / `FAIL` / `UNVERIFIED`
- 输出路径（绝对路径）：`<当前工作流>/最终产物/验收报告.md`
- 输出格式：中文主判定与逐项证据映射；机器码只作附注

不得读取完整聊天历史。
