# 需求基线

## 给用户的话

### 30 秒结论：确认我们理解的是同一个需求

这份基线用来确认目标、边界和成功标准是否与你的真实意图一致。它不会授权选择路线或开始实施。请优先检查有没有遗漏的场景、失败方式和明确不做的内容。确认后，我们才会进入路线比较。

## 我们要解决的问题

- 目标与成功信号：
- 用户 / 场景：
- 非目标：
- 失败、边界与例外：
- 验收信号：
- 已确认领域术语：
- 未决问题与后续验证项：

## 确认边界

- 用户需要确认：目标、非目标、约束、边界和验收信号是否一致
- 当前不能宣称：路线已选择、最终规格已批准或可以开始实施

## 证据与标识

- 版本：1
- 当前工作流目录（绝对路径，仅限 `~/.grill-harness/`）：
- 需求与约束：`REQ-001` / `CON-001`
- 关联需求雷达：`RAD-001`
- 阻塞项已清零：否；批准前必须确认所有 `blocking_level=baseline` 的当前 `RAD-*` 已关闭
- 规格前验证项：仍需在路线卡、仓库挑战、最终规格、任务、验收和知识沉淀中追踪相关 `RAD-*`
- 门禁：`requirements_baseline` / `needs_user`
- 用户确认记录：`<当前工作流>/过程产物/需求审问/用户确认记录.md`；待确认并绑定本版本与 `DEC-001`
