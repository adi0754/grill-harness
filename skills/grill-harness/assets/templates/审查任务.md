# `TASK-001` 审查任务

- 角色与目标：独立审查者
- 项目工作目录（绝对路径）：
- Git 基线：
- 必须读取：任务包、规格、完整文件、真实 diff
- Standards 轴：正确性、安全、维护性、性能、错误处理、测试质量、仓库规范
- Spec 轴：需求与决策覆盖、非目标、范围、兼容性、未验证假设
- 允许修改范围：默认只读产品代码
- 禁止修改范围：未获授权的产品修改
- 停止条件：基线不符、输入缺失、无法读取完整 diff
- 输出路径（绝对路径）：
- 输出格式：逐轴结论与稳定 `ISSUE-001`，附路径、符号和证据

不得读取完整聊天历史。
