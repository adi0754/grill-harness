# 知识变更预览

- 目标范围：项目知识 / 通用知识
- 预览 ID：`KPV-<内容哈希>`
- 新增：
- 去重：
- 冲突：
- 替代关系：旧知识通过 `replaced_by` 指向新的 `KNW-*`，不得覆盖历史。
- 已失效或已替代知识：仅解释历史，不参与当前规划。
- 当前独立验收证据：
- 项目归档用户批准：待绑定 `preview_id` 的 `DEC-*` / `CHG-*` 确认
- 通用知识第二批准：不适用 / 待另一个不同的 `DEC-*` / `CHG-*` 独立确认 / 已批准

展示本预览后停止；长期写入必须使用 `knowledge-promote --preview ... --approval-id ...`
引用对应批准。不能在生成预览的同一步写长期知识，也不能复用一次批准静默晋升通用知识。
