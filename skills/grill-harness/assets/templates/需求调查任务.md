# 需求调查任务

## 给用户的话

### 30 秒结论：先查清一个会影响需求基线的未知事实

这份任务只回答一个明确问题，不替用户作产品或风险取舍。调查者保持产品仓库只读，并在证据不足时明确停止。启动前由用户选择使用哪个模型或 Agent，也可以让主 Agent 继续。调查结论会回写需求基线相关产物，但不会自动推进门禁。

## 调查内容

- 调查理由：
- 调查问题：
- 角色：
- 预期产物：
- 是否阻塞需求基线：是 / 否
- 允许读取范围：
- 禁止修改范围：整个项目仓库与 Harness 状态
- 停止条件：需要产品取舍、风险容忍、范围决定或证据不足

## 用户控制

启动前停止。展示本任务后由用户选择使用哪个模型或 Agent，或让主 Agent 继续；不得自动派发多个调查者。

## 证据与标识

- 调查记录：`RAD-001`
- 项目工作目录（绝对路径，只读）：
- Git 基线：
- 输入证据与相关 `REQ-*` / `RAD-*`：
