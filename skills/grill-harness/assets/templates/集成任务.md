# 最终集成任务

## 给用户的话

### 30 秒结论：确认各切片合在一起仍然正确

集成者会检查任务之间的公共契约、配置、迁移顺序和端到端行为。单个任务各自通过，不代表组合后一定可以交付。只处理任务包已授权的集成修复，不增加新需求。发现共享契约冲突、基线漂移或需要产品取舍时立即停止。

## 集成内容

- 角色与目标：检查任务间集成，不新增产品范围
- 集成分支 / worktree 与合并顺序：
- 并行组、共享契约、迁移和生成物：
- 允许修改范围：经授权的集成修复
- 禁止修改范围：未批准需求
- 检查：冲突、公共契约、数据配置、重复实现、迁移顺序、跨模块流程、集成测试、全局覆盖
- 停止条件：共享契约冲突、基线漂移、需产品决策

## 证据与标识

- 项目工作目录（绝对路径）：
- 当前工作流目录（绝对路径，仅限 `~/.grill-harness/`）：
- Git 基线：
- 输入产物（逐项绝对路径与版本/基线）：最终规格、任务图、各实施与审查报告
- 输出路径（绝对路径）：`<当前工作流>/最终产物/集成报告.md`
- 输出格式：集成结论、实际 diff、`EVD-001` 证据与风险

不得读取完整聊天历史。
